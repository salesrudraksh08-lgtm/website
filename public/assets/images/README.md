# Website Image Assets

Replace these files to update the website visuals:

- `hero-bg.png` controls the homepage hero background and shared page-header background.
- `about-engineers.png` controls the About page image.
- `services-mepf.png` controls the Services page image and metrics background.

Keep the same file names when replacing images so the website updates automatically.
